// "=" an assignment operator
// "==" a comparison operator it is a loose equality operator because it perform type Coercion
// "===" the strict equality operator because it do not perform type Coercion

let age = '19'
if (age == 19) console.log('is equal') //not performing type Coercion

let dob = '8'
if (age === 8) console.log('is equal') //performing type Coercion

